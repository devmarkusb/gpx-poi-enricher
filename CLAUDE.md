# Claude Code — gpx-poi-enricher

Read **`AGENTS.md`** at the repository root first. It lists verified **uv** / **pytest** /
**pre-commit** commands, directory layout, safety boundaries, and the pre-merge checklist.

**Claude-specific:** This repository **gitignores `.claude/`** for local-only state. Do not ask
contributors to commit secrets or machine paths; keep shared workflow notes in `AGENTS.md` (and
this file) instead.
